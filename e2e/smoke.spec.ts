import{test,expect}from'@playwright/test';test('rotas principais não têm erros de console',async({page})=>{const errors:string[]=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});for(const route of['/','/login','/login?tab=cadastro','/questoes','/app']){await page.goto(route);await expect(page.locator('h1')).toBeVisible()}expect(errors).toEqual([])});test('login mock redireciona',async({page})=>{await page.goto('/login');await page.getByLabel('E-mail').fill('aluno@mente.com');await page.getByLabel('Senha',{exact:true}).fill('12345678');await page.getByRole('button',{name:'Entrar'}).click();await expect(page).toHaveURL(/\/app$/)});
