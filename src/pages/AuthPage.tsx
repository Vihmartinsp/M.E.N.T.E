import{useSearchParams}from'react-router-dom';import Logo from'../components/Logo';import AuthTabs from'../components/auth/AuthTabs';import{LoginForm,RegisterForm}from'../components/auth/AuthForms';
type Tab='entrar'|'cadastro';export default function AuthPage(){const[params,setParams]=useSearchParams();const active:Tab=params.get('tab')==='cadastro'?'cadastro':'entrar';const change=(tab:Tab)=>setParams({tab},{replace:true});return <main className="auth-page"><header className="auth-top"><Logo/></header><div className="auth-layout"><section className="auth-intro"><div><span>M.E.N.T.E</span><h2>Aprenda Matemática do ENEM de um jeito que faz sentido</h2><p>Crie sua conta gratuita e acompanhe seu progresso, pontuação e evolução a cada tópico.</p></div></section><section className="auth-panel"><div className="auth-box"><AuthTabs active={active} onChange={change}/>{active==='entrar'?<LoginForm switchTab={()=>change('cadastro')}/>:<RegisterForm switchTab={()=>change('entrar')}/>}</div></section></div></main>}
