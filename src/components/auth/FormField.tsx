import{Eye,EyeOff}from'lucide-react';type Props=React.InputHTMLAttributes<HTMLInputElement>&{label:string;error?:string;toggle?:()=>void;visible?:boolean};export default function FormField({label,error,toggle,visible,...input}:Props){const id=input.id!;return <div className="field"><label htmlFor={id}>{label}</label><div className="input-wrap"><input {...input} aria-invalid={!!error} aria-describedby={error?`${id}-error`:undefined}/>{toggle&&<button type="button" className="password-toggle" onClick={toggle} aria-label={visible?'Ocultar senha':'Mostrar senha'}>{visible?<EyeOff/>:<Eye/>}</button>}</div>{error&&<span className="error" id={`${id}-error`} role="alert">{error}</span>}</div>}
