type Tab='entrar'|'cadastro';export default function AuthTabs({active,onChange}:{active:Tab;onChange:(tab:Tab)=>void}){const key=(e:React.KeyboardEvent)=>{if(e.key==='ArrowRight'||e.key==='ArrowLeft')onChange(active==='entrar'?'cadastro':'entrar')};return <div className="auth-tabs" role="tablist" aria-label="Acesso à conta" onKeyDown={key}><button role="tab" aria-selected={active==='entrar'} tabIndex={active==='entrar'?0:-1} onClick={()=>onChange('entrar')}>Entrar</button><button role="tab" aria-selected={active==='cadastro'} tabIndex={active==='cadastro'?0:-1} onClick={()=>onChange('cadastro')}>Criar conta</button></div>}
