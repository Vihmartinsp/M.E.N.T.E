import{useState}from'react';import{Link}from'react-router-dom';import{Menu,X}from'lucide-react';import Logo from'./Logo';
export default function Header(){const[open,setOpen]=useState(false);const close=()=>setOpen(false);return <header className="site-header"><div className="container nav-wrap"><Logo/><button className="menu-toggle" aria-label={open?'Fechar menu':'Abrir menu'} aria-expanded={open} aria-controls="main-nav" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav id="main-nav" className={open?'main-nav open':'main-nav'} aria-label="Navegação principal"><a href="#sobre" onClick={close}>Sobre</a><a href="#funcionalidades" onClick={close}>Funcionalidades</a><Link className="btn btn-ghost" to="/login" onClick={close}>Entrar</Link><Link className="btn btn-primary" to="/questoes" onClick={close}>Começar agora</Link></nav></div></header>}
